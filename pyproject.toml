[project]
name = "agentar-fin-r1-reproduce"
version = "0.1.0"
description = "Reproduction of Agentar-Fin-R1: data pipeline + training + web stack"
requires-python = ">=3.10"

[tool.uv.workspace]
members = [
    "backend",
    "agentar-fin-r1/data",
    "agentar-fin-r1/training",
]
