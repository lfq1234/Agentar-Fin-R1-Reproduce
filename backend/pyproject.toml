[project]
name = "backend"
version = "0.1.0"
description = "FastAPI service + agent runtime for Agentar-Fin-R1 reproduction"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pydantic>=2.6",
    "httpx>=0.27",
]

[tool.uv]
package = false
