# 需求文档 · 多智能体基础框架（AgentScope）

> 编号：02
> 状态：已评审通过 · 已编码（v4：锁定 agentscope==0.1.6；专家/审核/风控/信息查询智能体 改用 ReActAgent；01 配置形态已对齐）
> 作者：
> 日期：2026-08-02（v4：编码落地 ReactAgent + 版本锁定 + 闭环 01 待改项）

## 1. 背景与诉求

后端确定用 **AgentScope** 做金融推理 multi-agent。上游 **01-模型接口** 已把 API / 本地两种模型统一成 `get_model()` 一个入口，并提供了统一调用方法 `generate()` 与配置导出 `build_agentscope_config()`。现有 `app/agent/runner.py` 是 stub，没有任何真正的 AgentScope 运行时。

本次只搭建 **multi-agent 基础框架本身**——它是后端 `app/agent/` 模块的地基，**独立于** `services/`（服务层）、`routes/`（接口层）、`db/`（数据层）。本框架只负责"把多智能体立起来、能端到端跑通一条对话"，由后续的**独立需求**（如服务层接入、HTTP 路由等）来消费/包装它。

> ✅ **AgentScope 版本（v4 已闭环）**：编码期实测确认——2.0.5 / 1.0.x **均无 `ReActAgent`**，经典版 `0.1.6` 才有；用户明确要求专家/审核/风控/RAG 使用 `ReActAgent`，故锁定 **`agentscope==0.1.6`**（即文档原假设的「v1」线）。01 的 `build_agentscope_config()` 已按 v1 形态（`generate_args`）改完，02 的 `ReActAgent` / `DialogAgent` 即其真实 API。本框架采用 §2 目标1 的「方案 X」，使 02 运行时只依赖 01 的 `generate()`，从而**不**直接依赖 AgentScope 的模型初始化 API。

> 范围校正（v2）：在 v1 的"1 协调者 + 5 领域专家"基础上，扩展 **3 个贯穿所有领域的支撑智能体**——信息查询智能体（本地 RAG 知识检索）、审核（合规）、风控（风险），形成完整流水线。

## 2. 目标与范围

- 目标 1：**02 的所有模型调用必须经由 01 的统一入口 `get_model()` 与统一调用方法 `generate()`**（评审门禁 G1，方案 X）——AgentScope 在本框架中**仅做多智能体编排/流水线**，模型执行不得绕过 01。具体落地：02 定义一个自定义模型包装器（如 `AgentarModel`），其模型调用委托给 `get_model().generate(prompt)`；所有 `DialogAgent`（或等价 agent）通过 `model=包装器` 注入，禁止 `DialogAgent` 自行直连模型。
- 目标 2：定义 **1 个 Coordinator**（协调 / 路由 / 意图识别，使用 `DialogAgent`）+ **5 个领域专家** `ReActAgent`（Banking / Securities / Insurance / Trust / MutualFunds）。
- 目标 3：定义 **3 个支撑智能体**（贯穿所有领域，非业务专家）：
  - **信息查询智能体（本地知识检索）**：从知识库 / 监管条文 / 产品库中检索依据，给领域专家"喂料"；**本期为接口占位**（检索后端留 stub，真实向量库为后续需求；联网新闻能力见 04）。
  - **审核（合规）**：对领域专家答案做合规审查（适当性、禁止承诺收益、风险提示到位、表述无误导），**建议式**（不阻断，附合规提示）。
  - **风控（风险）**：对答案做风险审查（风险等级匹配、高危操作预警），**建议式**（不阻断，附风险警示）。
- 目标 4：流水线编排——协调者按 `scene` 路由后，统一走 `信息查询智能体检索 → 领域专家 → 审核 → 风控 → 汇总`，全程由协调者驱动。
- 目标 5：提供统一运行入口 `run()`，返回 `AgentResult`（含 `reply` + `compliance_notes` + `risk_flags` + 结构化字段）；结构化分析（`intent/slots/tool_plan/expression`）本期作为框架能力，**不**对外暴露 HTTP。
- 目标 6：基础 prompts——Coordinator + 5 专家 + 信息查询智能体 + 审核 + 风控 共 **9 个** system prompt 文件（**简版 / 占位**，后续细化）。

**不在本次范围（均为后续独立需求，已"捏出来"排期）**：
- `services/` 服务层：把本框架接入 FastAPI、提供 `run_agent` 服务入口（→ 后续需求"后端服务接入"）。
- `routes/` 接口层：`/api/v1/chat`、`/api/v1/analyze` 的 HTTP 暴露（**统一 `/api` 前缀**）。
- `db/` 持久化、`agent_trace` 轨迹可视化、SSE 流式、真实金融工具注册表。
- **信息查询智能体真实检索后端**（向量库 / 知识库接入）：本期仅接口占位，留 stub。

## 3. 多 Agent 拓扑

```
用户消息
   │
   ▼
Coordinator（意图识别 + 场景路由 + 流水线驱动）
   │  scene = Banking/Securities/Insurance/Trust/MutualFunds（未给定则先判定）
   ▼
信息查询智能体（本地 RAG 检索；02 占位：retrieve() 返回 stub 上下文）
   │  检索上下文 → 注入领域专家
   ▼
领域专家 ReActAgent（按 scene，消费检索上下文作答，带 lookup_knowledge 工具）
   │  答案 draft —— 模型调用经 01 的 get_model().generate()（方案 X）
   ▼
审核（合规审查，建议式）→ 产出 compliance_notes
   │
   ▼
风控（风险审查，建议式）→ 产出 risk_flags
   │
   ▼
汇总 → AgentResult(reply + compliance_notes + risk_flags + 结构化字段)
```

- **领域专家**负责"作答"；**信息查询智能体 / 审核 / 风控**是通用支撑，不绑定具体业务领域。
- **审核 / 风控为建议式**：不拦截、不打回，专家答案正常返回，审查结论仅作为 `compliance_notes` / `risk_flags` 附在结果里。
- **信息查询智能体为接口占位**：02 中 `retrieve()` 返回空 / 占位上下文；真实检索后端（向量库等）由后续需求接入，本框架流水线结构保持不变。
- **统一模型入口（评审 G1）**：上述任一处对 LLM 的调用，都必须经由 01 的 `get_model().generate()`，禁止任何 agent 直接构造 OpenAI/本地客户端。

## 4. 功能 / 非功能要求

- 功能点（**全部位于 `app/agent/` 内**）：
  - 运行时初始化：经 `get_model()` 取得 01 统一模型对象；封装为 AgentScope 自定义模型包装器（模型调用委托给 `get_model().generate()`），再初始化 AgentScope 运行时。**02 不直接消费 `build_agentscope_config()` 来做模型初始化**（其形态对齐见 §6），但允许读取该配置作为包装器的辅助参数来源。
  - 智能体定义：Coordinator + 5 领域专家 + 信息查询智能体 + 审核 + 风控（共 **9 个角色**；信息查询智能体的检索后端为 stub）。
  - 流水线编排：`run(message, scene=None, structured=False) -> AgentResult`，封装初始化 / 构建 / 路由 / 信息查询智能体 / 审核 / 风控 / 汇总。
  - 基础 prompts：`agent/prompts/` 下 9 个 `.txt`（Coordinator / 5 专家 / rag / review / risk）。
  - 结构化分析：以 prompt 引导 LLM 输出 JSON，解析填充四个字段（框架级，不做严格 schema 校验）。
- 非功能约束：
  - **统一模型调用（评审 G1，硬约束）**：02 不得自行直连模型；所有 LLM 调用经 01 的 `get_model().generate()`。`model.mode` 在 `api` / `local` 间切换对框架透明（靠 01 内部切换），框架代码无需改动。
  - 错误契约（评审 N2）：02 捕获来自 `app.model` 的 `ModelInvokeError`（key 缺失 / 网络错误 / 超时），转化为框架级错误，**不得静默吞掉**。
  - AgentScope 版本对齐（评审 G2/G3 升级）：agent 构建与运行时须对齐 §6 锁定的 AgentScope 版本。
  - 可测性：无真实模型 / key 时，单测用 fake 模型或 `skip` 真实调用；RAG 检索后端因本就是 stub，可直接 mock。

## 5. 验收标准

- [ ] `app/agent/` 含 AgentScope 运行时初始化 + Coordinator + 5 专家 + 信息查询智能体 + 审核 + 风控构建 + 流水线编排（替换旧 `runner.py` stub）。
- [ ] **模型调用经 01**：代码审计 + 单测证明，02 对 LLM 的调用全部走 `get_model().generate()`，无 agent 自行构造 OpenAI/本地客户端（评审 G1）。
- [ ] 提供 `run()` 入口：返回 `reply`；建议式 `compliance_notes` / `risk_flags` 在结果中可见；`structured=True` 时返回 `intent/slots/tool_plan/expression`。
- [ ] `agent/prompts/` 含 9 个 prompt 文件（Coordinator + 5 专家 + rag + review + risk，简版）。
- [ ] 复用 01 `get_model()`：`model.mode` 在 `api` / `local` 间切换时，框架代码无需修改。
- [ ] `ModelInvokeError` 在 02 中有捕获与转化（非静默）。
- [ ] 单测：给定 `scene` 命中对应专家；流水线能在 `retrieve()` 返回 stub 时跑通；结构化输出可解析；无真实 key 时 `skip`。
- [ ] `services/`、`routes/`、`db/` 不被修改（`git diff` 仅 `app/agent/` + 测试）。

## 6. 01 待修改清单（02 编码前置硬依赖）

> 本节是 02 对上游 01 的**明确修改要求**，必须在 02 进入编码前由 01 完成（评审门禁 G2/G3 + 01 评审问题7）。01 当前代码 `app/model/{api/openai_api.py, local/transformer_local.py}` 的 `build_agentscope_config()` 仍输出**顶层 `temperature` / `stream`** 形态，不满足 AgentScope 配置契约。

### 6.1 锁定 AgentScope 版本（根因 · 已闭环 v4）

- **已锁定经典版 `agentscope==0.1.6`**（即文档原假设的「v1」线，含 `ReActAgent` / `DialogAgent` / `ServiceToolkit` / `agentscope.init`），写入 `pyproject.toml` 与 02 测试依赖。
- 编码期实测确认：2.0.5 / 1.0.x **均无 `ReActAgent`**，经典版 `0.1.x` 才有；用户明确要求专家/审核/风控/信息查询智能体 使用 `ReActAgent`，故版本钉死在 0.1.6（与文档原意一致）。
- 02 的 agent 构建（`ReActAgent` / `DialogAgent`）即 0.1.6 的真实 API；01 的 `build_agentscope_config()` 已按 v1 形态改完（见 §6.2）。
- 「方案 X」（模型调用走 `get_model().generate()`）与版本无关，始终成立。

### 6.2 `build_agentscope_config()` 必须产出对齐版本的正确形态

**v1 目标形态（若锁定 v1 线）**——将 `temperature` / `stream` 移入 `generate_args`：

```python
def build_agentscope_config(self) -> dict:
    return {
        "model_type": "openai_chat",
        "config_name": self.cfg.model_name,
        "model_name": self.cfg.model_name,
        "api_key": self.cfg.api_key,
        "base_url": self.cfg.base_url,
        "generate_args": {
            "temperature": self.cfg.temperature,
            "stream": self.cfg.stream,
        },
    }
```

**2.x 目标形态（若锁定 2.0.5，已实测 `OpenAIChatModel` 接受的键）**——`type` / `credential` / `model` / `parameters`：

```python
def build_agentscope_config(self) -> dict:
    return {
        "type": "openai_chat",
        "credential": {
            "api_key": self.cfg.api_key,
            "base_url": self.cfg.base_url,
        },
        "model": self.cfg.model_name,
        "parameters": {
            "temperature": self.cfg.temperature,
        },
        "stream": self.cfg.stream,
    }
```

### 6.3 字段名与 `base_url` 键（评审 G3 + 01 评审问题7）

- `config_name` / `model_name` 是否都为必需、`base_url` 在配置里是顶层键还是 `credential`/`client_kwargs` 子键，**取决于锁定的版本**（见 §6.1）。01 须在锁版后，以该版本 `OpenAIChatModel`/`OpenAIChatWrapper` 实际接受的签名为准回填，闭环 01 评审问题7。
- 本地 vLLM 的 `base_url`（如 `http://localhost:8000/v1`）须与所选版本一致地透传（v1 顶层 `base_url`；2.x 置于 `credential.base_url`）。

### 6.4 01 待改小结（给 01 作者的 action list）

1. 在 `pyproject.toml` 锁定 AgentScope 版本（与 02 协商）。
2. 按锁定版本改写 `app/model/api/openai_api.py` 与 `app/model/local/transformer_local.py` 的 `build_agentscope_config()`（§6.2）。
3. 以锁定版本核实 `config_name` / `model_name` / `base_url` 键名并回填（闭环 01 评审问题7 / 02 门禁 G3）。
4. 上述三项完成后，02 方可基于「01 配置契约已对齐」进入编码；02 自身运行时仍只依赖 `get_model().generate()`。

## 7. 产品审核说明（评审修复记录）

| # | 严重度 | 评审问题 | 02 侧处理 | 状态 |
|---|---|---|---|---|
| G1 | 门禁·高 | 02 只调 `build_agentscope_config()`，未调 `generate()`，模型执行绕过 01 | 已落定 **方案 X**：02 必须调用 `get_model().generate()`，自定义包装器委托调用，AgentScope 仅做编排（需求 §2 目标1、§3、§4、§5 验收） | 已闭环（02 侧） |
| G2 | 门禁·高 | `build_agentscope_config()` 形态不符（顶层 `temperature/stream`） | **已闭环**：版本锁定 `agentscope==0.1.6`（v1 线），01 的 `build_agentscope_config()` 已改为 v1 形态（`generate_args`，见 §6.2） | 已闭环 |
| G3 | 门禁·中 | 本地 vLLM `base_url` 透传键名未定 | 随 §6.1/§6.3 在锁版后由 01 核实回填 | 转 01 处理 |
| N1 | 非阻塞·中 | 角色计数 8 vs 9 | 需求 §4 / 技术 §10 统一为 **9 个角色** | 已修复 |
| N2 | 非阻塞·中 | `ModelInvokeError` 在 02 无桥接 | 02 捕获 `app.model.ModelInvokeError` 并转化，非静默（需求 §4 非功能、§5 验收） | 已修复 |
| N3 | 非阻塞·低 | "01 待修订项"误以为改文档即可 | 全文改为「01 待修改（**代码层**）」，并写入 §6 具体代码改动 | 已修复 |

**产品侧结论**：02 设计已自洽、可测试、可验收；唯一阻塞为上游 01 的配置契约前置修改（§6），不属 02 自身缺陷。建议：待 01 完成 §6 后，02 转「已评审通过 · 可编码」。

## 8. 关联

- 技术文档：同文件夹 `技术文档.md`
- 评审文档：同文件夹 `评审文档.md`
- 总文档：`../00-总文档/00-总文档.md`（⚠️ 总文档 §7 当前将"多 Agent 编排"放在 `services/agent_runtime.py`，与本项目"框架在 `app/agent/`"的层位不一致；且总文档 §7.1 仅描述 5 专家拓扑，待 02 评审通过后回总文档校正：编排归属 `app/agent/`，并补充 信息查询智能体 / 审核 / 风控 支撑角色）
- 上游依赖：**01-模型接口**（`get_model()` + `generate()` 统一调用；`build_agentscope_config()` 配置导出——其形态须按本文件 §6 由 01 修正）
- 下游（后续独立需求）：信息查询智能体真实检索后端、服务层接入、HTTP 路由暴露、DB 持久化、`agent_trace` 可视化、金融工具注册表
