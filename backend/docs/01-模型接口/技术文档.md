# 技术文档 · 模型接口（API + 本地双模式）

> 编号：01
> 状态：已评审通过（根据评审文档修订，进入编码）
> 作者：
> 日期：2026-08-01（修订：2026-08-02）

## 1. 需求背景与目标
对应需求文档：在 `app/model/` 建立统一模型接口层，支持 API 与本地（vLLM）两种调用方式、物理分离、配置切换、工厂分发。目标是把"模型怎么调"和"Agent 怎么编排"解耦，让 `services/` 只依赖一个抽象接口。

## 2. 影响范围
- [x] `config/`（YAML 配置变更：新增 `model.mode`、`model.api`、`model.local` 三段）
- [x] `model/`（api / local 调用方式：新增 `base.py`、`exceptions.py`、`factory.py`、`api/`、`local/`）
- [ ] `db/`（本次不涉及）
- [ ] `services/`（仅作为下游消费方，本次不改编排，仅预留接口）
- [ ] `routes/`（本次不改接口）
- [ ] `agent/prompts/`（本次不涉及）

## 3. 接口 / 数据结构变更

### 3.1 目录结构（新增）
```
app/model/
├── __init__.py            # 导出 ModelInterface, ModelConfig, get_model, ModelInvokeError
├── base.py                # ModelInterface(abc)：抽象接口；ModelConfig(dataclass)
├── exceptions.py          # ModelInvokeError：统一异常
├── factory.py             # get_model() -> ApiModel | LocalModel（校验 mode + 合并默认）
├── api/
│   ├── __init__.py
│   └── openai_api.py      # ApiModel：云端 OpenAI 兼容
└── local/
    ├── __init__.py
    └── vllm_local.py      # LocalModel：本地 vLLM OpenAI 兼容
```

### 3.2 抽象接口（`model/base.py`）
```python
from abc import ABC, abstractmethod
from dataclasses import dataclass

@dataclass
class ModelConfig:
    model_type: str          # 固定 "openai_chat"
    model_name: str
    api_key: str
    base_url: str
    temperature: float = 0.3
    stream: bool = False
    timeout: int = 120
    max_retries: int = 2

class ModelInterface(ABC):
    def __init__(self, cfg: ModelConfig): ...
    @abstractmethod
    def build_agentscope_config(self) -> dict:
        """返回 AgentScope openai_chat 模型配置字典。"""
    @abstractmethod
    def generate(self, prompt: str, **kwargs) -> str: ...
```
> 异常：`generate()` 在 key 缺失 / 网络错误 / 超时时抛出 `ModelInvokeError`（见 §3.6、§3.3）。

### 3.3 `ApiModel` / `LocalModel`（实现要点）
两者仅 `base_url` / `api_key` 来源不同（`ApiModel` 取 `config.model.api`，`LocalModel` 取 `config.model.local`）。`build_agentscope_config()` 统一产出：
```python
{
    "model_type": "openai_chat",
    "config_name": self.cfg.model_name,
    "model_name": self.cfg.model_name,
    "api_key": self.cfg.api_key,
    "base_url": self.cfg.base_url,
    "temperature": self.cfg.temperature,
    "stream": self.cfg.stream,
}
```
> **字段名门禁（评审问题7）**：`openai_chat` 的字段名（`config_name` vs `model_name`）随 AgentScope 版本变化。**编码前必须 `pip show agentscope` + 官方 tutorial 锁定字段名，并将确认结果回填本节、删除本提示**，否则验收"返回合法 openai_chat 配置"无法判定。

`generate()` 实现策略（评审问题3，已决策）：本期 `generate()` **直连 OpenAI 兼容端点**——`ApiModel` 用真实 key 调云端 `base_url`，`LocalModel` 同样用 OpenAI 兼容客户端指向 vLLM 的 `http://localhost:8000/v1`；`build_agentscope_config()` 仅作为给后续 `services/` AgentScope 编排的"配置导出"，不影响 `generate()` 行为。

`ModelInvokeError` 触发（评审问题4）：`generate()` 在以下场景抛出 `ModelInvokeError`：
- `api_key` 为占位/空（`local` 的 `EMPTY` 在直连场景下视为缺失）；
- 网络错误（连接失败 / 5xx / 超时）；
- 超过 `max_retries` 仍失败。

### 3.4 工厂（`model/factory.py`）
```python
from app.config import config
from app.model.exceptions import ModelInvokeError
from app.model.api.openai_api import ApiModel
from app.model.local.vllm_local import LocalModel

def get_model() -> ModelInterface:
    mode = config.model.get("mode")
    if mode not in ("api", "local"):                       # 评审问题5：非法 mode 显式报错
        raise ValueError(f"model.mode 必须是 'api' 或 'local'，收到: {mode!r}")
    section = dict(config.model.get(mode))                 # mode=local -> model.local，否则 model.api
    # 评审问题1：合并顶层全局默认 timeout / max_retries，子段优先
    for key in ("timeout", "max_retries"):
        if key not in section and key in config.model:
            section[key] = config.model[key]
    cls = LocalModel if mode == "local" else ApiModel
    return cls(ModelConfig(**section))
```

### 3.5 配置（`config/config.yaml` 相关段）
```yaml
model:
  mode: api                  # api | local
  timeout: 120               # 全局默认，工厂合并进所选子段
  max_retries: 2
  api:
    model_type: openai_chat
    model_name: gpt-4o
    api_key: ${OPENAI_API_KEY}
    base_url: https://api.openai.com/v1
    temperature: 0.3
    stream: false
  local:
    model_type: openai_chat
    model_name: qwen3-8b-finr1
    api_key: EMPTY
    base_url: http://localhost:8000/v1
    temperature: 0.3
    stream: false
```
> 密钥用 `${ENV_VAR}` 引用，加载时替换；提交 `config.example.yaml` 脱敏模板。顶层 `timeout`/`max_retries` 为全局默认，由工厂合并进 `api`/`local` 子段（评审问题1 修复）。

### 3.6 依赖变更（评审问题6）
- `openai>=1.x`：本期 `generate()` 直连所需（OpenAI 兼容客户端，可指向 vLLM `/v1`）。
- `agentscope`：本期仅用于 `build_agentscope_config()` 的字段导出参考；**编码前 `pip show agentscope` 核实版本**，并据此锁定 §3.3 字段名（评审问题7 门禁）。

## 4. 实现要点与步骤
1. **`config/` 配置落地**：YAML 加载器递归替换 `${ENV_VAR}`；在 `config.yaml` 补齐 `model` 三段（含顶层 `timeout`/`max_retries`），同步 `config.example.yaml`。
2. **`model/base.py`**：定义 `ModelConfig`（dataclass）与 `ModelInterface`（ABC）。
3. **`model/exceptions.py`**：定义 `ModelInvokeError(RuntimeError)`。
4. **`model/api/openai_api.py`**：`ApiModel(ModelInterface)`，构造接收 `ModelConfig`；`generate()` 用 `openai` 客户端直连云端 `base_url`，失败时抛 `ModelInvokeError`。
5. **`model/local/vllm_local.py`**：`LocalModel(ModelInterface)`，逻辑同 `ApiModel`，端点为本地 vLLM；`api_key == "EMPTY"` 时直连场景视为缺失并抛 `ModelInvokeError`。
6. **`model/factory.py`**：`get_model()` 校验 `mode` + 合并全局默认 + 返回实现（见 §3.4）。
7. **`model/__init__.py`**：导出 `ModelInterface`、`ModelConfig`、`get_model`、`ModelInvokeError`。
8. **`tests/test_model.py`**：覆盖工厂分发、mode 校验、配置产出、异常（见 §5）。
9. **编码前门禁**：`pip show agentscope` 锁定 `openai_chat` 字段名并回填 §3.3。

## 5. 测试与验收标准
- [ ] `tests/test_model.py`：`config.model["mode"]` 置 `api` → `get_model()` 返回 `ApiModel`；置 `local` → 返回 `LocalModel`；置非法值（如 `local2`）→ 抛 `ValueError`（评审问题5）。
- [ ] 两种实现 `build_agentscope_config()` 返回的字典 `model_type=="openai_chat"`，`base_url` 分别等于配置中的云端 / 本地端点（字段名以编码前核实结果为准，评审问题7）。
- [ ] 配置加载：注入 `OPENAI_API_KEY=sk-test` 后，`ApiModel` 配置中的 `api_key` 被正确替换（非 `${OPENAI_API_KEY}` 字面量）。
- [ ] 超时/重试生效（评审问题1）：`ModelConfig.timeout`/`max_retries` 实际取到顶层全局默认或被子段覆盖，而非永远默认值。
- [ ] 异常可识别（评审问题4）：`api_key` 缺失 / 网络不可达时，`generate()` 抛 `ModelInvokeError`。
- [ ] 导入冒烟：`from app.model import get_model, ModelInterface, ModelInvokeError` 无异常。

## 6. 风险与待定
- **AgentScope 字段名差异**：`openai_chat` 配置键随版本变化，已列为**编码前门禁**（§3.3、§3.6），核实后回填，避免硬编码错键。
- **本地模式依赖 vLLM 服务**：`local` 模式需外部已起 vLLM 端点；开发期先用 `api` 模式把链路跑通，再切 `local`。
- **`generate()` 已被决策为直连**：不再悬而未决；主链路由后续 `services/` 经 AgentScope 调用时复用 `build_agentscope_config()` 导出（评审问题3 已闭环）。
- **超时 / 重试**：本期 `timeout`/`max_retries` 透传配置并经工厂合并（§3.4），具体重试策略后续在 `services/` 统一处理。

## 7. 关联
- 需求文档：同文件夹 `需求文档.md`
- 评审文档：同文件夹 `评审文档.md`
- 总文档：`../00-总文档/00-总文档.md`（本期交付记录）
- 下游：后续 `services/`（任务清单）将 `import` 本层的 `get_model()` 构建 AgentScope 模型配置。

## 8. 评审问题修复记录
| 评审问题 | 内容 | 修复方式 | 状态 |
|---|---|---|---|
| 1 死配置 | 顶层 timeout/max_retries 未传入工厂 | 工厂合并全局默认（§3.4/§3.5）；验收增加检查 | 已修复 |
| 3 generate() | 实现策略未决策 | 定为直连 OpenAI SDK（§3.3/§2.5） | 已修复 |
| 4 异常 | ModelInvokeError 未设计 | 新增 exceptions.py 并定义触发场景（§3.2/§3.3/§3.6） | 已修复 |
| 5 非法 mode | 静默回退 api | 工厂校验 mode，非法抛 ValueError（§3.4/§5） | 已修复 |
| 6 依赖 | 未声明新增依赖 | 新增 §3.6 列 openai / agentscope | 已修复 |
| 7 字段名 | openai_chat 字段名悬空 | 列为编码前门禁，核实后回填 §3.3（§3.3/§5） | 门禁·编码前 |
| 2 单测文件名 | 需求未对齐 | 需求文档 §4 明确 `tests/test_model.py`（见需求文档） | 已修复 |
| 8 需求矛盾 | "共用wrapper" vs "物理分离" | 需求文档 §3 澄清（见需求文档） | 已修复 |
